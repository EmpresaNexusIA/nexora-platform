# ai/embeddings/

Scripts y configuración del pipeline de embeddings (de `ai/knowledge/` a
Qdrant). Se completa en el sub-proyecto 5 (Capa de IA), sobre la base de
Qdrant construida en el sub-proyecto 2 (Capa de datos).

Vacía a propósito en el sub-proyecto 1.
