# monitoring/

Reservada para el sub-proyecto 8 (Seguridad y operaciones): compose files de
Prometheus (métricas), Grafana (dashboards) y Loki + Promtail (logs
centralizados), siguiendo el mismo patrón de compose modular que `infra/`.

Vacía a propósito en el sub-proyecto 1 (infra base).
